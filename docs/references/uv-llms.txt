面向 AI 贡献者的 uv 参考

当前状态：
- `uv` 不是这个仓库当前工具链的一部分
- 仓库基于 Node.js 与 pnpm

不要使用 `uv` 来做这些事情：
- 安装依赖
- 执行脚本
- 管理当前项目环境

请改用：
- `source ~/.nvm/nvm.sh`
- `pnpm install`
- `pnpm lint`
- `pnpm type-check`
- `pnpm test:e2e`

为什么要保留这个文件：
- 文档系统需要为常见工具保留紧凑参考入口
- 对“当前未使用”做显式说明，比留白更安全
